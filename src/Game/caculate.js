const constraint = require('../constraint')

class Caculate{
    constructor(num1 =0, num2=0, caculator, level=1){
        this.num1 = num1 ;
        this.num2 = num2 ;
        this.caculator = caculator
        this.level = level 
    }

    init(level){
        // switch
    }
}